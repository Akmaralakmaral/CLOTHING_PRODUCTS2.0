@model List<CLOTHING_PRODUCTS.Models.Role>

@{
    ViewData["Title"] = "All Roles";
}

<h2>@ViewData["Title"]</h2>

 @*@if (ViewContext.HttpContext.Session.GetString("UserRole") != "Директор")
                {
<p>
    <a asp-action="Create" class="btn btn-primary">Create New Role</a>
</p>
                }*@

@if (Model != null && Model.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Role ID</th>
                <th>Role Name</th>
                <th>Login Name</th>
                <th>Password</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var role in Model)
            {
                <tr>
                    <td>@role.RoleId</td>
                    <td>@role.RoleName</td>
                    @if (ViewContext.HttpContext.Session.GetString("UserRole") != "Директор")
                    {
                        <td>
                            <div style="display: inline-block;">
                            <a asp-controller="KURSOVAYARole" asp-action="Edit" asp-route-id="@role.RoleId" class="btn btn-warning">Edit</a> |
                        </div>
                       @* <div style="display: inline-block;">
                            <form asp-action="Delete" asp-route-id="@role.RoleId" method="post" onsubmit="return confirm('Are you sure you want to delete this employee?');">
                                <button type="submit" class="btn btn-danger">Delete</button>
                            </form>
                        </div>*@
                    </td>
                    }
                </tr>
           
            }
        </tbody>
    </table>
}
else
{
    <p>No roles found.</p>
}
